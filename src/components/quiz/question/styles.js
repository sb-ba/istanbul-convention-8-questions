import css from 'styled-jsx/css';

export default css`
  .question {
    color: white;
  }

  .title {
    font-size: 1.4rem;
  }

  label {
    display: block;
  }

  label + label {
    margin-top: 1rem;
  }
`;
