import css from 'styled-jsx/css';

export default css`
  nav {
    align-items: center;
    background: white;
    bottom: 0;
    display: flex;
    justify-content: space-between;
    left: 0;
    position: fixed;
    width: 100%;
    z-index: 10;
  }
`;
